
  <scroll-view style="height: {{height + 'px'}}" scroll-y bindscrolltolower="loadmore" class="body-wrapper">
    <view class="index-container">
      <image class="banner" src="../assets/index/banner.png"/>
      <view class="header">
        <view class="logo-wrapper">
          <image src="../assets/index/logo.png" class="logo"/>
        </view>
        <view class="search-wrapper">
          <input type="text" placeholder="搜索职位" class="input-inner" bindchange="change" data-wpychange-a="post" data-wpychange-b="params"/>
          <image class="search-icon" src="../assets/index/search.png"/>
        </view>
      </view>
    </view>
    <view class="index-container">
      <view class="action">
        <view class="action-item">
          <picker bindchange="change" class="weui-input" range="{{types}}" data-wpychange-a="type" data-wpychange-b="types">
            <text>{{params.type}}</text>
            <image src="../assets/index/arrow-down.png"/>
          </picker>
        </view>
        <view class="action-item">
          <text>薪酬优先</text>
        </view>
        <view class="action-item">
          <text>区域选择</text>
        </view>
        <view class="action-item">
          <text>筛选</text>
          <image src="../assets/index/arrow-down.png"/>
        </view>
      </view>
    </view>
    <view class="index-container">
      <block wx:for="{{jobs}}" wx:for-index="index" wx:for-item="job" wx:key="index">
        <view class="jobs">
          
  <view class="job-item" bindtap="$job$go" data-com-index="{{index}}">
    <view class="job-detail no-border">
      <view class="avatar-wrapper">
        <image src="{{job.avatar}}" class="avatar"/>
      </view>
      <view class="detail-wrapper">
        <text class="block grey">{{labels.industry}}: {{job.industry}}</text>
        <view class="date grey">
          <text>{{job.date}}</text>
        </view>
        <text class="title block">{{job.title}}</text>
        <text class="intro block grey">{{job.intro}}</text>
        <view class="salary"  wx:if="{{!$job$simple}}">
          <text>{{job.salary}}</text>
        </view>
        <view class="status">
          <text>{{job.statusText}}</text>
        </view>
      </view>
    </view>
    <view class="job-action-list"  wx:if="{{!$job$simple}}">
      <view class="job-action-item">
        <text class="block grey">{{labels.contact}}</text>
        <text class="block dark">{{job.contact}}</text>
      </view>
      <view class="job-action-item">
        <text class="block grey">{{labels.post}}</text>
        <text class="block dark">{{job.post}}</text>
      </view>
      <view class="job-action-item">
        <text class="block grey">{{labels.tel}}</text>
        <text class="block dark">{{job.tel}}</text>
      </view>
      <view class="job-action-item">
        <image src="../assets/index/phone.png" class="phone"/>
      </view>
    </view>
  </view>

        </view>
      </block>
    </view>
  </scroll-view>