
  <view>
    <view class="index-container">
      <block wx:for="{{resumeList}}" wx:for-index="index" wx:for-item="resume" wx:key="index">
        <view class="jobs">
          
  <view class="job-item" bindtap="$resume$go" data-com-index="{{index}}">
    <view class="job-detail no-border">
      <view class="avatar-wrapper">
        <image src="{{resume.imgArray && resume.imgArray[0]}}" class="avatar"/>
      </view>
      <view class="detail-wrapper">
        <text class="block grey">{{resume.school}}</text>
        <!-- <view class='date grey'>
          <text>{{resume.date}}</text>
        </view> -->
        <text class="title block">{{resume.username}}</text>
        <text class="intro block grey">{{resume.specialty + '专业'}}</text>
        <view class="salary">
          <text>{{resume.tel}}</text>
        </view>
      </view>
    </view>
  </view>

        </view>
      </block>
    </view>
    
    <view class="wetoast {{$toast$reveal ? 'wetoast_show' : ''}}">
        <view class="wetoast__mask"/>
        <view class="wetoast__bd" animation="{{$toast$animationData}}">
            <block  wx:if="{{$toast$img}}">
                <view class="wetoast__bd__img {{$toast$imgClassName || ''}}">
                    <image class="wetoast__bd__img_desc" src="{{$toast$img}}" mode="{{$toast$imgMode || 'scaleToFill'}}"/>
                </view>
            </block>
            <block  wx:if="{{$toast$title}}">
                <view class="wetoast__bd__title {{$toast$titleClassName || ''}}">{{$toast$title}}</view>
            </block>
        </view>
    </view>

  </view>